## What changed

<!-- one or two lines -->

## Checklist

- [ ] `npm test` is green (90 checks)
- [ ] New user-facing text went into `brief` (one line) or `note`, not both
- [ ] New functions: the short name was added to `GROUPS` (see `CONTRIBUTING.md`)
- [ ] Anything network-touching sets `net: true`, so the offline run skips it
- [ ] Start-up was considered — `node test/cli.mjs` carries the lazy-loading guards
